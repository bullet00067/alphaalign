這是一份整合了所有討論細節、最完整的「互動式資產再平衡與監控平台」產品需求規格書（PRD）。本版本已將「動態比例調整介面」、「單次試算/永久儲存雙軌機制」、「交易成本估算」以及「雙重驗證報價系統」全數納入。

---

# 產品需求規格書 (PRD)：互動式資產再平衡與監控平台

**文件版本：** v1.1

**部署環境：** Web Application (部署於 Render 平台)

**開發主軸：** 100% 免費開源套件、雲端原生架構、高彈性自訂配置

---

## 1. 產品概述 (Product Overview)

本產品為一個專為個人投資者設計的**動態資產監控與再平衡（Portfolio Rebalancing）系統**。旨在消除手動計算資產權重與買賣單位的繁瑣流程。透過直覺的動態互動介面、嚴格的雙重報價驗證機制，在使用者設定的再平衡週期（如每季、每半年、每年）或臨時需要調整策略時，精確計算出包含交易成本在內的具體「買入/賣出單位」執行清單，並長期追蹤資產成長趨勢。

---

## 2. 核心功能需求 (Core Functional Requirements)

### 2.1 動態配置與互動調整介面 (UI/UX & Dynamic Allocation)

* **靈活配置卡片：** 使用者可自由新增資產類別（例如：市值型股票、高股息型、債券、現金），且每個類別下可綁定多檔具體標的代號（Ticker，如 `QQQM`, `QYLD`, `2330.TW`）。
* **待分配資金池機制 (Unallocated Pool)：**
* 介面提供加減按鈕（步進值 1%）與數字輸入框。
* 上方即時顯示配置總和（例：`95% / 100%`），並計算出「尚有 5% 待分配」或「超額配置 -2%」之狀態提示。


* **防呆鎖定邏輯：** 當配置總和**不等於 100%** 時，系統將顯示警示紅字，並將下方的執行按鈕完全禁用（Disabled）。

### 2.2 試算與儲存之雙軌機制 (Dual-Action Execution)

當配置總和達到 100% 時，介面必須提供兩個獨立的操作按鈕，滿足短期戰術模擬與長期戰略變更：

1. **【僅單次試算】(Simulate Only)：**
* **行為：** 前端將當下調整好的草稿比例（Draft Allocation）直接打包傳送至後端計算引擎。
* **影響：** 僅在畫面上產出本次再平衡的买卖建議。不寫入資料庫，重新整理網頁後即恢復原設定。


2. **【更新目標配置並產出報告】(Save Target & Rebalance)：**
* **行為：** 同時觸發資料庫寫入請求與再平衡計算。
* **影響：** 永久覆寫資料庫中的目標權重，未來登入皆以新比例為準，並產出再平衡建議。



### 2.3 雙重驗證報價系統 (Dual-Verification Pricing System)

為確保買賣建議報告的絕對精準，系統在產出報告前的運算階段，必須啟動價格雙重驗證：

* **主要來源：** 透過 `yfinance` 等金融數據套件獲取標的的即時/延時報價。
* **次要來源（爬蟲驗證）：** 後端非同步啟動輕量級爬蟲，直接訪問該標的之 Yahoo Finance 網頁連結，抓取網頁即時價格。
* **核對邏輯：** 兩者數據進行二次比對，若有顯著偏差（例如 API 延遲），系統自動以 Yahoo Finance 網頁爬取之最新價格為主要計算基準，確保買賣建議不因數據落後而失真。

### 2.4 再平衡計算引擎與交易成本估算 (Calculation Engine & Fees)

* **淨值計算公式：** $\text{Total NAV} = \text{現金總額} + \sum (\text{各資產持有股數} \times \text{已驗證即時股價}) + \text{本次預計加碼資金 (Deposit)}$
* **執行動作換算：**
* $\text{目標類別金額} = \text{Total NAV} \times \text{自訂目標比例}$
* $\text{金額差額} = \text{目標類別金額} - \text{該類別目前實際市值}$
* **正差額 (+)：** 產出 **「買入」** 指令。買入單位數 = $\text{差額} \div \text{即時股價}$。
* **負差額 (-)：** 產出 **「賣出」** 指令。賣出單位數 = $|\text{差額}| \div \text{即時股價}$。


* **交易成本整合：**
* **台股標的：** 計算時預扣 0.1425% 券商手續費與 0.3% 賣出證交稅。
* **美股標的：** 計算時依據設定檔扣除海外券商固定手續費或複委託成本。
* **單位處理：** 台股標的自動向下取整至個位數股（或提示轉為張數）；美股標的若券商支援碎股，則計算至小數點後兩位。



### 2.5 資產監控中心與歷史快照 (Dashboard & History Tracking)

* **即時儀表板：** 登入後自動加載最新數據，透過圖表（如 Chart.js/ECharts）直覺呈現「調整前佔比 vs. 目標佔比」的偏離度。
* **資產成長曲線：** 每當使用者執行再平衡並確認「已實際交易」後，系統會為當前的 Total NAV 與資產配置存入歷史快照，於前端繪製長線的資產成長趨勢圖。

---

## 3. 系統技術架構 (System Architecture)

全面採用雲端原生與前後端分離架構，完美匹配 Render 平台之特性：

* **前端 (Frontend Site)：** 採用 **React** 或 **Vue 3** 搭配 **Tailwind CSS**，部署為 Render 的 **Static Site**（享有高頻寬、全球 CDN 且永不休眠）。
* **後端 (Backend Service)：** 採用 **Python (FastAPI)**，負責計算演算法、API 接口與驗證爬蟲。封裝為 **Dockerfile** 部署於 Render **Web Service**。
* **資料庫 (Database)：** 使用 Render 的 **Managed PostgreSQL**，儲存使用者帳戶、持有資產快照、歷史再平衡紀錄。

---

## 4. API 設計概要 (API Routes Outline)

* `GET /api/portfolio` - 獲取當前所有資產庫存與目標比例。
* `POST /api/rebalance/calculate` - **【僅單次試算】**。接收臨時比例與金額，執行雙重驗證並回傳買賣建議（不更動資料庫）。
* `PUT /api/allocations/targets` - **【儲存設定】**。更新資料庫中該用戶的目標配置比例。

---

## 5. 資料庫邏輯模型 (Database Schema Draft)

1. **`allocations` (類別設定表)：** `id` (UUID), `category_name` (VARCHAR), `target_pct` (NUMERIC), `updated_at` (TIMESTAMP)
2. **`assets` (具體標的表)：** `id` (UUID), `allocation_id` (FK), `ticker` (VARCHAR), `shares` (NUMERIC), `currency` (VARCHAR)
3. **`history_snapshots` (歷史趨勢表)：** `id` (UUID), `record_date` (DATE), `total_nav` (NUMERIC), `snapshot_data` (JSONB)

---

這份 PRD 全面定義了平台的運作邏輯與技術藍圖。你可以進行 review，看看是否符合你對於這個平台的整體想像？若確認無誤，我們接下來就可以針對**後端計算與 Yahoo Finance 雙重驗證的 Python 核心代碼**來做實作討論！